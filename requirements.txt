# Phase 0 baseline dependencies
# (We’ll add Torch/Whisper/CLIP + LangGraph/RAG in later phases once Python compatibility is locked.)
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9
pydantic==2.7.0
python-dotenv==1.0.0
httpx==0.27.0
